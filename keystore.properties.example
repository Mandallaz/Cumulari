# Copy this file to keystore.properties (gitignored) and fill in real values
# to produce a signed release build locally. Never commit the real file or
# the .jks keystore itself.
storeFile=keystore/cumulari-upload.jks
storePassword=changeit
keyAlias=cumulari-upload
keyPassword=changeit
